import { describe, it } from "jsr:@std/testing/bdd";
import { expect } from "jsr:@std/expect";
import { solve } from "./index.ts";

describe("Reto 26: PIEDRA, PAPEL, TIJERA", () => {
  it(" ", () => {;
    expect(solve()).toBe(0);
  });
});
